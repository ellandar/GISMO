//
// Source code generated by Celerio, a Jaxio product.
// Documentation: http://www.jaxio.com/documentation/celerio/
// Follow us on twitter: @jaxiosoft
// Need commercial support ? Contact us: info@jaxio.com
// Template pack-angular:web/src/app/entities/entity.service.ts.e.vm
//
import { Injectable } from '@angular/core';
import { HttpClient, HttpResponse, HttpErrorResponse } from '@angular/common/http';
import { LazyLoadEvent } from 'primeng/primeng';
import { MessageService } from '../../service/message.service';
import { PageResponse, PageRequestByExample } from '../../support/paging';
import { CapitalSubtype } from './capitalSubtype';
import { Observable } from 'rxjs/Rx';
import { catchError, map } from 'rxjs/operators';
import 'rxjs/add/observable/throw';

@Injectable()
export class CapitalSubtypeService {

    constructor(private http: HttpClient, private messageService : MessageService) {}

    /**
     * Get a CapitalSubtype by id.
     */
    getCapitalSubtype(id : any) : Observable<CapitalSubtype> {
        return this.http.get('/api/capitalSubtypes/' + id)
            .pipe(
                map(response => new CapitalSubtype(response)),
                catchError(this.handleError)
            );
    }

    /**
     * Update the passed capitalSubtype.
     */
    update(capitalSubtype : CapitalSubtype) : Observable<CapitalSubtype> {
        let body = capitalSubtype;

        return this.http.put('/api/capitalSubtypes/', body)
            .pipe(
                map(response => new CapitalSubtype(response)),
                catchError(this.handleError)
            );
    }

    /**
     * Load a page (for paginated datatable) of CapitalSubtype using the passed
     * capitalSubtype as an example for the search by example facility.
     */
    getPage(capitalSubtype : CapitalSubtype, event : LazyLoadEvent) : Observable<PageResponse<CapitalSubtype>> {
        let req = new PageRequestByExample(capitalSubtype, event);
        let body = req;

        return this.http.post<PageResponse<any>>('/api/capitalSubtypes/page', body)
            .pipe(
                map(pr =>  new PageResponse<CapitalSubtype>(pr.totalPages, pr.totalElements, CapitalSubtype.toArray(pr.content))),
                catchError(this.handleError)
            );
    }

    /**
     * Performs a search by example on 1 attribute (defined on server side) and returns at most 10 results.
     * Used by CapitalSubtypeCompleteComponent.
     */
    complete(query : string) : Observable<CapitalSubtype[]> {
        let body = {'query': query, 'maxResults': 10};
        return this.http.post<any[]>('/api/capitalSubtypes/complete', body)
            .pipe(
                map(response => CapitalSubtype.toArray(response)),
                catchError(this.handleError)
            );
    }

    /**
     * Delete an CapitalSubtype by id.
     */
    delete(id : any) {
        return this.http.delete('/api/capitalSubtypes/' + id)
            .pipe(catchError(this.handleError));
    }

    // sample method from angular doc
    private handleError (error: HttpErrorResponse) {
        // TODO: seems we cannot use messageService from here...
        let errMsg = (error.message) ? error.message : 'Server error';
        console.error(errMsg);
        if (error.status === 401 ) {
            window.location.href = '/';
        }
        return Observable.throw(errMsg);
    }
}
