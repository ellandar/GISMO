/*
 * Source code generated by Celerio, a Jaxio product.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Follow us on twitter: @jaxiosoft
 * Need commercial support ? Contact us: info@jaxio.com
 * Template pack-angular:src/main/java/rest/EntityResource.java.e.vm
 */
package com.apside.gismo.rest;

import static org.springframework.http.MediaType.APPLICATION_JSON_VALUE;
import static org.springframework.web.bind.annotation.RequestMethod.DELETE;
import static org.springframework.web.bind.annotation.RequestMethod.GET;
import static org.springframework.web.bind.annotation.RequestMethod.POST;
import static org.springframework.web.bind.annotation.RequestMethod.PUT;

import java.net.URI;
import java.net.URISyntaxException;
import java.util.List;
import java.util.Optional;

import javax.inject.Inject;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.bind.annotation.RequestBody;

import com.apside.gismo.domain.GismoUser;
import com.apside.gismo.dto.GismoUserDTO;
import com.apside.gismo.dto.GismoUserDTOService;
import com.apside.gismo.dto.support.PageRequestByExample;
import com.apside.gismo.dto.support.PageResponse;
import com.apside.gismo.repository.GismoUserRepository;
import com.apside.gismo.rest.support.AutoCompleteQuery;

@RestController
@RequestMapping("/api/gismoUsers")
public class GismoUserResource {

    private final Logger log = LoggerFactory.getLogger(GismoUserResource.class);

    @Inject
    private GismoUserRepository gismoUserRepository;
    @Inject
    private GismoUserDTOService gismoUserDTOService;

    /**
     * Create a new GismoUser.
     */
    @RequestMapping(value = "/", method = POST, produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<GismoUserDTO> create(@RequestBody GismoUserDTO gismoUserDTO) throws URISyntaxException {

        log.debug("Create GismoUserDTO : {}", gismoUserDTO);

        if (gismoUserDTO.isIdSet()) {
            return ResponseEntity.badRequest().header("Failure", "Cannot create GismoUser with existing ID").body(null);
        }

        GismoUserDTO result = gismoUserDTOService.save(gismoUserDTO);

        return ResponseEntity.created(new URI("/api/gismoUsers/" + result.id)).body(result);
    }

    /**
    * Find by id GismoUser.
    */
    @RequestMapping(value = "/{id}", method = GET, produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<GismoUserDTO> findById(@PathVariable Integer id) throws URISyntaxException {

        log.debug("Find by id GismoUser : {}", id);

        return Optional.ofNullable(gismoUserDTOService.findOne(id)).map(gismoUserDTO -> new ResponseEntity<>(gismoUserDTO, HttpStatus.OK))
                .orElse(new ResponseEntity<>(HttpStatus.NOT_FOUND));
    }

    /**
     * Update GismoUser.
     */
    @RequestMapping(value = "/", method = PUT, produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<GismoUserDTO> update(@RequestBody GismoUserDTO gismoUserDTO) throws URISyntaxException {

        log.debug("Update GismoUserDTO : {}", gismoUserDTO);

        if (!gismoUserDTO.isIdSet()) {
            return create(gismoUserDTO);
        }

        GismoUserDTO result = gismoUserDTOService.save(gismoUserDTO);

        return ResponseEntity.ok().body(result);
    }

    /**
     * Find a Page of GismoUser using query by example.
     */
    @RequestMapping(value = "/page", method = POST, produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<PageResponse<GismoUserDTO>> findAll(@RequestBody PageRequestByExample<GismoUserDTO> prbe) throws URISyntaxException {
        PageResponse<GismoUserDTO> pageResponse = gismoUserDTOService.findAll(prbe);
        return new ResponseEntity<>(pageResponse, new HttpHeaders(), HttpStatus.OK);
    }

    /**
    * Auto complete support.
    */
    @RequestMapping(value = "/complete", method = POST, produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<List<GismoUserDTO>> complete(@RequestBody AutoCompleteQuery acq) throws URISyntaxException {

        List<GismoUserDTO> results = gismoUserDTOService.complete(acq.query, acq.maxResults);

        return new ResponseEntity<>(results, new HttpHeaders(), HttpStatus.OK);
    }

    /**
     * Delete by id GismoUser.
     */
    @RequestMapping(value = "/{id}", method = DELETE, produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<Void> delete(@PathVariable Integer id) throws URISyntaxException {

        log.debug("Delete by id GismoUser : {}", id);

        try {
            gismoUserRepository.delete(id);
            return ResponseEntity.ok().build();
        } catch (Exception x) {
            // todo: dig exception, most likely org.hibernate.exception.ConstraintViolationException
            return ResponseEntity.status(HttpStatus.CONFLICT).build();
        }
    }
}